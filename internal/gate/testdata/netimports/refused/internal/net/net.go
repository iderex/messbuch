// Package net is the one package permitted to reach a network-capable API.
// Here it does not, so the only difference between this tree and the accepted
// one beside it is which file the import sits in.
package net

// Name exists so the package is not empty.
const Name = "net"
