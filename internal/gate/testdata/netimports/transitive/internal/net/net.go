// Package net reaches nothing here, so the refusal in this tree cannot be
// mistaken for one about the permitted package.
package net

// Name exists so the package is not empty.
const Name = "net"
