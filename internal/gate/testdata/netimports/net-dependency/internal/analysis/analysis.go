// Package analysis reaches nothing network-capable and imports nothing of the
// permitted package, so it is the package this tree asserts is left alone.
package analysis

// Name exists so the package is not empty.
const Name = "analysis"
