// Package analysis reaches nothing network-capable in its source. Its test
// file does, which is the arm that would escape a graph computed without the
// test packages.
package analysis

// Name exists so the package is not empty.
const Name = "analysis"
